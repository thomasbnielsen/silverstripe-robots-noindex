#Define if site is indexed or not

Block site from beeing indexed (for example for staged sites). 
Shows a warning in backend (now via silverstripe infoboxes) so you remember to set to index when going live
